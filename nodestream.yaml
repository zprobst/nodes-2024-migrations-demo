scopes:
  default:
    pipelines:
      - pipelines/social.yaml
      # - pipelines/people.yaml
      # - pipelines/locations.yaml

targets:
  my-db:
    database: neo4j
    uri: bolt://localhost:7687
    username: neo4j
    password: neo4j123
    